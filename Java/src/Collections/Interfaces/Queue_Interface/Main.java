package Collections.Interfaces.Queue_Interface;

/**
 *           Note on Queue
 *
 *           ~ Is a Collection designed for holding elements prior to processing
 *           ~ Unlike List or Set, a Queue is about processing order
 *           ~ The core idea behind it is FIFO: First In, First Out
 *
 *           Core Methods
 *            add()/remove()/element() - Exception style
 *            offer()/poll()/peek() - Safe style
 *
 *            If you're building high-performance backend systems:
 *            → ArrayDeque for normal FIFO
 * → PriorityQueue for scheduling
 * → ConcurrentLinkedQueue for multi-threaded
 *
 * When using Queue, ask:
 *
 * Is ordering important?
 *
 * Do I need priority?
 *
 * Is it multi-threaded?
 *
 * Is memory usage critical?
 *
 * What’s the expected size?
 */
public class Main {
}
